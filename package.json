{
  "name": "walletconnect-poc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "deploy": "npm run clean && vite build && git add dist -f && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "@web3modal/ethers": "^5.1.11",
    "ethers": "^6.13.5",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "rimraf": "^6.0.1",
    "vite": "^6.0.5"
  }
}
